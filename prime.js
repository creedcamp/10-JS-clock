const colorChange = setInterval(() => {
  let TI = new Date()
  let seconds = TI.getSeconds
  let colors = "#count".color[i]
  document.querySelector("#count").innerHTML = seconds + colors
}, 1000)

const change = setTimeout(() => {
  if (setInterval)
  let colors = "#count".color
  document.querySelector("#count").innerHTML = seconds + colors++
  console.log("check")
}, 2000)
